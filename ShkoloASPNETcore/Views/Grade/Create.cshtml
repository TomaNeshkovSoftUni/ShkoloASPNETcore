@model ShkoloASPNETcore.Infrastructure.Data.Models.Grade
@{
    ViewData["Title"] = "Добави Оценка";
}

<div class="container mt-4">
    <h2 class="text-uppercase fw-bold border-bottom border-dark pb-2 mb-4">Добавяне на Оценка</h2>

    <div class="card p-4 shadow-sm" style="max-width: 600px;">
        <form asp-action="Create" novalidate="novalidate">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3 fw-bold"></div>

            <div class="mb-3">
                <label asp-for="StudentId" class="form-label fw-bold">Ученик (Student)</label>
                <select asp-for="StudentId" class="form-select" asp-items="ViewBag.Students" required>
                    <option value="">-- Избери Ученик --</option>
                </select>
            </div>

            <div class="mb-3">
                <label asp-for="SubjectId" class="form-label fw-bold">Предмет (Subject)</label>
                <select asp-for="SubjectId" class="form-select" asp-items="ViewBag.Subjects" required>
                    <option value="">-- Избери Предмет --</option>
                </select>
            </div>

            <div class="mb-4">
                <label asp-for="Value" class="form-label fw-bold">Оценка</label>
                <input asp-for="Value" class="form-control border-dark" type="text" placeholder="напр. 5.50 или 6" required />
            </div>

            <div class="d-flex gap-2 mt-2 border-top border-dark pt-3">
                <button type="submit" class="btn btn-dark px-4 fw-bold">ЗАПАЗИ</button>
                <a asp-action="Index" class="btn btn-outline-dark px-4 fw-bold">ОТКАЗ</a>
            </div>
        </form>
    </div>
</div>